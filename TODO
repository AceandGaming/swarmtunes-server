# In progress
- Migration tool (pain)

# Todo

# Testing
- Run long term test with all features active
- Test metadata parser

# Before push to prod
- Make tool to add songs
- Update dockerfile, pyproject.toml, etc
- Update docs + README

# Post Release
- OAuth (Google, Discord)
- HLS
- Create new v2 endpoint with new api stuff
- Auto fetch from YT (somehow)
- Make artwork automatic
- Search for artists/albums (V2 api)
- Proper playlist sharing (user permisson list + read-only acess. V2 Api)
